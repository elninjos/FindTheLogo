package Logic;

import android.content.res.AssetManager;

import java.io.IOException;

public class BackgroundFunctions {

    public static void GetImagesData(AssetManager assets) throws IOException {
  /*      ObjectMapper mapper = new ObjectMapper();
        InputStream is = assets.open("imagesData.json");
        List<ImageData> participantJsonList = mapper.readValue(jsonString, new TypeReference<List<Student>>(){});
        int a = 5;*/
    }
}
